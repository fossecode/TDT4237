{% extends 'base.twig' %}

{% block content %}

<h2>User</h2>

{{ flash.info }} <br />
{% if flash.errors %}
<ul>
    {% for error in flash.errors %}
        <li> {{ error|escape }} </li>
    {% endfor %}
</ul>
{% endif %}

{% if foundUser %}
    <table>
        <tr>
            <th>Id</th>
            <th>{{ foundUser.userid|escape }}</th>
            <tr></tr>
            <th>Username:</th>
            <td>{{ foundUser.username|escape }}</th>
            <tr></tr>
            <th>Bio:</th>
            <td>{{ foundUser.bio|escape }}</td>
            <tr></tr>
            <th>Age:</th>
            <td>{{ foundUser.age|escape }}</td>
            <tr></tr>




    </table>
{% else %}
    Could not find a user with username "{{ username|escape }}".
{% endif %}

{% endblock %}
